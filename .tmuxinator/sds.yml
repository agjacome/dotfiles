# ~/.tmuxinator/sds.yml

name: sds
root: ~/src/sing/sds

windows:
  - vim  : vim -c 'set wildignore=target,lib,logs,share,doc'
  - sbt  : export SBT_OPTS=$(cat .sbtopts) && sbt
  - log  : less +F logs/application.log
  - git  : git status
  - bash : ls
