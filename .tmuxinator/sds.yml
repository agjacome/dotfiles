# ~/.tmuxinator/sds.yml

name: sds
root: ~/projects/sing/sds

windows:
  - vim  : vim -c 'set wildignore=*target*'
  - sbt  : export SBT_OPTS=$(cat .sbtopts) && sbt
  - log  : less +F logs/application.log
  - git  : git status
  - bash : ls
