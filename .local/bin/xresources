#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

declare -r is_docked=${1:-$($HOME/bin/is-docked)}

xrdb -load "$HOME/.Xresources"

if [[ $is_docked == "true" ]]; then
    xrdb -merge -nocpp "$HOME/.Xresources_docked_overrides"
fi
