#!/bin/bash
set -euo pipefail

declare -r directory="$HOME/media/pictures/wallpapers"

declare -r count=$(ls -1 "$directory" | wc -l)
declare -r random=$(( 1 + $RANDOM % $count ))

feh --bg-fill --no-fehbg -- "${directory}/${random}.jpg"
