#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

declare -r is_docked=${1:-$($HOME/.local/bin/_xutils/is-docked)}

if [[ $is_docked == "true" ]]; then
    feh --no-fehbg --bg-fill "$HOME/media/pictures/wallpapers/docked"
else
    feh --no-fehbg --bg-fill "$HOME/media/pictures/wallpapers/undocked"
fi
